# تقرير المتغيرات — تطبيق «تقرير التصحيح والتطوير»

**آخر تحديث:** 2026-06-03  
**المرجع:** `تقرير التصحيح والتطوير.pdf`  
**التقرير الموحّد:** [`تقرير_المتغيرات_والموانع_والمتطلبات.md`](تقرير_المتغيرات_والموانع_والمتطلبات.md)  
**المستودع:** https://github.com/alasermohamad94/cyber

---

## ملخص تنفيذي

| المرحلة | الحالة |
|---------|--------|
| P0–P2 | مكتمل |
| P3 (ML Shadow) | جزئي |
| واجهة عربية احترافية (أخضر/ذهبي) | **مكتمل (2026-06)** |
| RBAC (admin / analyst / viewer) | **مكتمل ومُختبر (2026-06)** |
| نشر Render | **جاهز** — `render.yaml` + `RENDER_DEPLOY.md` |
| الاختبارات | **22/22** ناجحة |

---

## P0 — الأمان التشغيلي

| المتغير | قبل | بعد |
|---------|-----|-----|
| مصادقة API | غير موجودة | جلسة Flask + `@api_login_required` / `@require_permission` |
| AuthZ (أدوار) | — | admin / analyst / viewer — API **403** عند المخالفة |
| الدور في الجلسة | من `session['role']` فقط | **مصدر الحقيقة:** قاموس المستخدمين في `get_user_directory()` |
| `SECRET_KEY` | ثابت | `CDS_SECRET_KEY` من البيئة |
| CORS / الربط | `*` و `0.0.0.0` | `CDS_CORS_ORIGINS` + `CDS_BIND_HOST` |
| حظر IP | تسجيل فقط | `security/firewall.py` + SQLite |
| تسجيل الدخول | — | `login.html` + تحميل `.env` تلقائياً |

**صلاحيات الأدوار (مُطبَّقة في API + واجهة):**

| الدور | قراءة | تحليل كيان | حظر IP | تقرير | إعدادات |
|------|-------|------------|--------|-------|---------|
| admin | ✅ | ✅ | ✅ | ✅ | ✅ |
| analyst | ✅ | ✅ | ❌ | ✅ | ❌ |
| viewer | ✅ | ❌ | ❌ | ❌ | ❌ |

---

## P1 — مصداقية بيانات الواجهة

| الصفحة | المصدر |
|--------|--------|
| `analytics.html` | `/api/analytics-overview` |
| `threat_management.html` | `/api/threat-overview` + `/api/blocked-ips` |
| عقد المؤشرات | `metrics_contract.py` + `/api/metrics-contract` |
| جودة البيانات | `data_quality` في الاستجابات + شريط علوي |
| Demo | `CDS_DEMO_MODE` + `/api/demo/disclaimer` |

---

## P2 — متانة النظام

SQLite دائم، `RLock`، `CpuSampler`، `background_queue`، `run_web_dashboard.py` مع `chdir` صحيح.

---

## P3 — ذكاء اصطناعي (جزئي)

`model_inference.py` + Isolation Forest + Shadow Mode في `main.py`. LSTM/XGBoost — انظر `الموانع.md`.

---

## تحديثات 2026-06-03 (واجهة + صلاحيات + نشر)

| الموضوع | التفاصيل |
|---------|----------|
| **تصميم** | `cds-pro.css` + `cds-app.js` — RTL عربي، ألوان `#002623` / `#b9a779` |
| **تخطيط** | شريط جانبي، بطاقات KPI، رسوم بيانات بألوان الغابة |
| **RBAC واجهة** | `data-permission` + Jinja `cds_has_perm` + `body.cds-role-*` |
| **جلسة** | `/api/session-info` يعيد `role` + `permissions[]` |
| **دخول** | جدول كلمات المرور الفعلية في صفحة Login؛ رفض أسماء مستخدمين مكررة في `.env` |
| **ملف `.env`** | يُحمَّل من جذر المشروع (غير مرفوع لـ Git) |
| **Render** | `render.yaml`, `RENDER_DEPLOY.md`, `wsgi.py` لـ gunicorn |
| **اختبارات RBAC** | تلاعب جلسة، viewer/analyst 403، تسجيل دخول viewer |

---

## ملفات جديدة / محدّثة (أبرزها)

**جديد:** `web_dashboard/static/css/cds-pro.css`, `web_dashboard/static/js/cds-app.js`, `render.yaml`, `RENDER_DEPLOY.md`

**محدّث:** `security/auth.py`, `security/config.py`, `security/roles.py`, قوالب HTML، `web_dashboard/app.py`, `tests/test_security_integration.py`, `.env.example`

---

## بيانات الدخول (افتراضية — غيّرها قبل النشر)

| المستخدم | كلمة المرور | الصلاحية |
|----------|-------------|----------|
| `admin` | `changeme` | كامل |
| `analyst` | `analyst123` | تحليل + تقارير |
| `viewer` | `viewer123` | قراءة فقط |

> إذا نسخت `.env.example` قديماً كانت كلمة المدير `change-this-password`. الصفحة `/login` تعرض **الكلمات الفعلية** على الجهاز.

---

## تشغيل محلي

```powershell
cd web_dashboard
python run_web_dashboard.py
```

| الصفحة | الرابط |
|--------|--------|
| دخول | http://127.0.0.1:8080/login |
| لوحة التحكم | http://127.0.0.1:8080/web_dashboard |

---

## نشر Render (مجاني)

راجع [`RENDER_DEPLOY.md`](RENDER_DEPLOY.md). بعد `git push` إلى `main`: Render → Manual Deploy أو Auto-Deploy.

---

## اختبارات

```powershell
pytest tests/
```

**النتيجة:** 22/22 ناجحة.

---

## ملاحظات

1. الواجهة الرسمية: **`web_dashboard/`** — `dashboard/` CLI مُهمَل (`DEPRECATED.md`).
2. جدار الحماية على OS يحتاج صلاحيات مسؤول.
3. ارفع التغييرات: `git push origin main` (يتطلب مصادقة GitHub).
